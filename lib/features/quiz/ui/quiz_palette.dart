import 'package:flutter/material.dart';

/// Shared brand-ish accents used in the quiz flows.
const Color quizWhatsAppGreen = Color(0xFF25D366);
// Use solid black instead of the previous deep green/orange accent.
const Color quizWhatsAppTeal = Color(0xFF000000);
